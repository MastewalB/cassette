import 'package:flutter/material.dart';


class PodcastEpisodeSmall extends StatelessWidget {
  const PodcastEpisodeSmall({Key? key,
  required this.children
  }) : super(key: key);
  final List<Widget> children;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
